{
  "name": "typescript-lambda-xray",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx esbuild --bundle handler.ts --outdir=dist --platform=node"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.72",
    "esbuild": "^0.8.54",
    "typescript": "^4.2.2"
  },
  "dependencies": {
    "aws-embedded-metrics": "^2.0.4",
    "aws-lambda": "^1.0.6",
    "aws-sdk": "^2.855.0",
    "aws-xray-sdk": "^3.2.0",
    "axios": "^0.21.1"
  }
}
